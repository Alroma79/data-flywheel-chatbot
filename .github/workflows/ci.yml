name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: false
        tags: flywheel:test
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    - name: Run tests in container
      run: |
        docker run --rm \
          -e OPENAI_API_KEY=sk-test-key-for-testing \
          -e DATABASE_URL=sqlite:///./test_chatbot.db \
          -e DEBUG=true \
          -e PYTHONPATH=/app/backend \
          flywheel:test python -m pytest tests/ -q --tb=short
          
    - name: Test Docker Compose
      run: |
        echo "OPENAI_API_KEY=sk-test-key-for-testing" > .env.test
        echo "DATABASE_URL=sqlite:///./test_chatbot.db" >> .env.test
        echo "DEBUG=true" >> .env.test
        docker compose --env-file .env.test run --rm test
        
    - name: Verify application starts
      run: |
        docker run -d --name test-app -p 8000:8000 \
          -e OPENAI_API_KEY=sk-test-key-for-testing \
          -e DATABASE_URL=sqlite:///./test_chatbot.db \
          -e DEBUG=true \
          flywheel:test
        sleep 15
        curl -f http://localhost:8000/health || (docker logs test-app && exit 1)
        docker stop test-app
        docker rm test-app
